Django==5.2.16
djangorestframework==3.16.1

# Auth (SIMPLE_JWT in base.py)
djangorestframework-simplejwt==5.5.1

# Database
psycopg2-binary==2.9.12

# Env vars (environ.Env / DATABASE_URL / REDIS_URL in base.py)
django-environ==0.14.0

# Cache (CACHES in base.py)
django-redis==7.0.0
redis==8.0.1

# CORS (corsheaders in MIDDLEWARE + INSTALLED_APPS)
django-cors-headers==4.9.0

# Filtering / search / ordering (DEFAULT_FILTER_BACKENDS)
django-filter==26.1

# OpenAPI schema (DEFAULT_SCHEMA_CLASS + SPECTACULAR_SETTINGS)
drf-spectacular==0.30.0

# Celery + DB-backed periodic tasks (CELERY_BEAT_SCHEDULER)
celery==5.6.3
django-celery-beat==2.9.0